"use client";

// Social proof widget removed — was showing fake purchase/review notifications.
// Keeping the file to avoid import errors; renders nothing.
export default function SocialProof() {
  return null;
}
